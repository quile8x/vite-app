import {
  JsonRpcProvider,
  Signer,
  StaticJsonRpcProvider,
  Web3Provider
} from "./chunk-6C6BZV6I.js";
import {
  init_define_process_env
} from "./chunk-2PHUQY7K.js";

// node_modules/eth-hooks/chunk-ABR2QVOF.js
init_define_process_env();
var asyncSome = async (arr, predicate) => {
  for (let e of arr)
    if (await predicate(e))
      return e;
};
var parseProviderOrSigner = async (providerOrSigner) => {
  let signer, provider, providerNetwork;
  if (providerOrSigner && (providerOrSigner instanceof JsonRpcProvider || providerOrSigner instanceof Web3Provider || providerOrSigner instanceof StaticJsonRpcProvider)) {
    let accounts = await providerOrSigner.listAccounts();
    accounts && accounts.length > 0 && (signer = providerOrSigner.getSigner()), provider = providerOrSigner, providerNetwork = await providerOrSigner.getNetwork();
  }
  return !signer && providerOrSigner instanceof Signer && (signer = providerOrSigner, provider = signer.provider, providerNetwork = provider && await provider.getNetwork()), { signer, provider, providerNetwork };
};

export {
  asyncSome,
  parseProviderOrSigner
};
//# sourceMappingURL=chunk-TCHFCXA2.js.map
