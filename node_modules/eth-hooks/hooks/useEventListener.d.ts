import { Contract, EventFilter } from 'ethers';
import { Result } from 'ethers/lib/utils';
import { TypedEvent } from "../models";
/**
 * #### Summary
 * Tracks the events of associated with a contract
 *
 * #### Notes
 * - updates triggered through ethers event listener
 * - uses the current provider {@link ethersProvider} from {@link useEthersContext}
 *
 * @category Hooks
 *
 * @param contract ethers.Contract
 * @param eventName
 * @param startBlock
 * @returns
 */
export declare const useEventListener: (contract: Contract | undefined, eventName: string | EventFilter, startBlock: number) => TypedEvent<Result>[];
//# sourceMappingURL=useEventListener.d.ts.map